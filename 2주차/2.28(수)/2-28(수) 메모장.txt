스택
배열

링크드 리스트 만들기 -> 데이터가 들어갈 장소, 다음 노드를 가리키는 장소

class Node {
	private int data;
	private Node nextnode;
}

노드를 가지고 리스트를 구현. add 시 링크드 리스트 연결
remove() 메소드 구현

1. list 인터페이스 (타입) 정의
삽입, 삭제, 가져오기
add, remove(int index), get(int index)

2. ArrayList implements list {
내부 데이터는 배열

3. 링크드 리스트 구현
LinkedList

4. main 함수에서
List list = LinkedList();
list.add()
list.remove(1)
list.get(1) => int 

에서 작동하게끔 코드를 만들어라
+> linkedlist.java

ㅡㅡㅡㅡㅡ

실습).
클래스 계층 구조 구현
Member	-> Student		-> Graduator
		-> Professor

리스트에서 정렬이 가능해야 함.
어떤 클래스가 Comparable이 되어야 비교 가능하겠는가?

MemberList 에는 아무거나 들어올 수 있다.
Iteration 가능, 클래스 안의 객체는 정렬이 가능해야 한다.


ex). 쇼핑몰
Product	-> ProductDetail

ProductList
는 Iterable이어야 함. 정렬도 가능해야 한다.

상품 목록 (상품 번호, 이름, 가격)
상품 상세

??. 자바에서 하나의 객체가 여러 비교목록으로 비교가 가능한가?
=> Comparator 사용 : 외부에서 비교조건 만든 후 리스트에 던지기